{
  "name": "apn-monorepo",
  "private": true,
  "packageManager": "pnpm@8.15.4",
  "scripts": {
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "dev": "pnpm -r --parallel dev"
  },
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
